(defun projectile-load-workspace ()
  "Loads the a saved workspace from a '.projectile-workspace.el' file inside the
project root directory."
  )

(defun projectile-store-workspace ()
  "Stores the current window configuration to a '.projectile-workspace.el' file in
the project root directory."
  )

(defun projectile-store-and-purge-workspace ()
  "Stores the current workspace and closes all project specific files."
  )
